import 'package:flutter/cupertino.dart';

// MARK: Empty widget.
const emptyWidget = SizedBox();

// MARK: Horizontal boxes.
const wBox8 = SizedBox(width: 8);
const wBox16 = SizedBox(width: 16);

// MARK: Vertical boxes.
const hBox8 = SizedBox(height: 8);
const hBox20 = SizedBox(height: 20);

// MARK: Paddings.
const allPadding8 = EdgeInsets.all(8.0);
const allPadding16 = EdgeInsets.all(16.0);

const hPadding16 = EdgeInsets.symmetric(horizontal: 16);

const bottomPadding8 = EdgeInsets.only(bottom: 8);

// MARK: Border radius.
const borderAll16 = BorderRadius.all(Radius.circular(16));
const borderAll20 = BorderRadius.all(Radius.circular(20));

// MARK: Duration.
const duration200Milliseconds = Duration(milliseconds: 200);
const duration300Milliseconds = Duration(milliseconds: 300);
const duration1800Milliseconds = Duration(milliseconds: 1800);
const duration5Seconds = Duration(seconds: 5);
